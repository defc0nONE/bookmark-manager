<%= styled_flash %>

<ul>
  <% @links.each do |link| %>
    <li><%= link %></li>
    <li><%= link.url %></li>
  <% end %>
</ul>

<form action='/url' method=post>
  <input type='text' name='new_url'>
  <input type='submit' value='Submit'>
</form>
